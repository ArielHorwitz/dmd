---
name: closeout
description: Close out a session by surfacing only unresolved loose ends that would otherwise be lost.
argument-hint: "Optional: emphasis or scope to narrow the closeout"
disable-model-invocation: true
---

I intend to close this session. Surface *only* unresolved loose ends or material context that isn't recorded elsewhere and would otherwise be lost when this session ends — including next-session guidance that meets that bar. If nothing qualifies, respond only: `Clean closeout.`

This is *not* a recap, status report, handoff, or summary of completed work. Keep the response concise and skimmable — no wall of text. Present each item as one bullet: a short bold label, then a brief note on why it's still open. Each note stays to one short line; I'll pull on the threads that matter.

```
- **not merged**: you mentioned wanting to do this later
- **response lag**: we never actually addressed this
- **missing log file**: I raised this but it was never handled
```

If arguments are passed, treat them as added emphasis or a narrower scope for the closeout.
